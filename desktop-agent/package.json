{
  "name": "dms-desktop-agent",
  "version": "1.0.0",
  "description": "Filesystem Agent for the Document Management Platform - brokers file operations on a machine the backend cannot reach directly",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^33.2.0"
  }
}
